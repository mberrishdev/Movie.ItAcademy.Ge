@model List<Movie.BO.Web.MVC.Models.Account.UserViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1>User Roles</h1>
<table class="table table-striped">
    @Html.AntiForgeryToken()
    <thead>
        <tr>
            <th>UserId</th>
            <th>UserName</th>
            <th>Email</th>
            <th>Roles</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.UserName</td>
                <td>@user.Email</td>
                <td>@string.Join(" , ", user.Roles.ToList())</td>
                <td>
                    <a class="btn btn-primary" asp-controller="User" asp-action="Delete" asp-route-Id="@user.Id">Delete</a>
                </td>
                <td>
                    <a class="btn btn-primary" asp-controller="User" asp-action="Update" asp-route-Id="@user.Id">Update</a>
                </td>
            </tr>
        }
    </tbody>
</table>